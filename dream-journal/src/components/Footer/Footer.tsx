import Button from "../Button/Button.tsx";

function Footer() {
  return (
    <footer>
      <Button className="add" sameWidthHeight>
        +
      </Button>
    </footer>
  );
}

export default Footer;
